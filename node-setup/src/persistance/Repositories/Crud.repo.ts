// interface CrudRepo {
//     // create(user:): boolean,
//     // getById(id: string): boolean,
//     // update(id: string, user): boolean,
//     // delete(id: string): boolean,
// }

// export default CrudRepo;
